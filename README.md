# Beyond the Burn

A tiny Hello World website ready to deploy on Netlify.

## Deploy

This repository is a static site. Netlify can publish it directly from the repo root using the included `netlify.toml`.
